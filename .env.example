OPENAI_API_KEY=
MODEL=gpt-4o-mini
# Sampling temperature. 0 = deterministic (recommended for SQL + reproducible
# evals). Some models (gpt-5 / reasoning family) reject an explicit temperature:
# set LLM_TEMPERATURE=default (or leave it blank) to omit it entirely.
LLM_TEMPERATURE=0
